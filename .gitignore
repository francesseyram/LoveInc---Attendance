# Dependencies & build output
node_modules/
dist/

# Environment & secrets — never commit real keys (use `.env.example` as the template)
.env
.env.local
.env.sandbox
.env.development.local
.env.test.local
.env.production.local

# Firebase CLI local cache
.firebase/

# OS junk
.DS_Store
Thumbs.db

# Editors / IDEs
.idea/
.vscode/
*.swp
*.swo

# AI assistant local files — never commit, never attribute
CLAUDE.md
CLAUDE.local.md
.claude/
.claude.json
.claude.json.backup
.mcp.json

# Logs & package manager debug
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Test / deploy tooling (if added later)
coverage/
.vercel/
*.tsbuildinfo

# Member roster PII — real names, phone numbers, emails, birthdays.
# Keep out of git unless this repo is private and the team agrees to it.
scripts/members-seed.json
members_for_review.csv
